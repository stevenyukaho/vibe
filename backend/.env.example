# Backend configuration
PORT=5000
HOST=localhost

# CrewAI agent service URL (Python service, default port 5002)
# This is only used for direct CrewAI execution paths.
AGENT_SERVICE_URL=http://localhost:5002
# AGENT_SERVICE_TIMEOUT=0

# Database configuration
DB_PATH=./data/agent-testing.db